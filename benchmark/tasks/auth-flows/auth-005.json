{
  "id": "auth-005",
  "category": "auth-flows",
  "difficulty": "medium",
  "intent": "On LinkedIn, verify you are logged in by finding the messaging icon or navigation element that only appears for authenticated users",
  "start_url": "https://www.linkedin.com",
  "requires": {
    "tools": ["safari_navigate", "safari_snapshot", "safari_get_text"],
    "engines": [],
    "auth_domains": ["linkedin.com"],
    "features": [],
    "competitive": false
  },
  "eval": {
    "type": "structured_output",
    "schema": {
      "type": "object",
      "properties": {
        "logged_in": { "type": "boolean" }
      },
      "required": ["logged_in"]
    }
  },
  "timeout_ms": 45000,
  "max_budget_usd": 0.25,
  "tags": ["auth-flows", "linkedin", "login-check", "messaging", "live"]
}
