{
  "id": "wf-009",
  "category": "workflows",
  "difficulty": "hard",
  "intent": "Search Wikipedia for each of the three major browser engines \u2014 WebKit, Blink, and Gecko \u2014 and extract the initial release year for each from its article",
  "start_url": "https://en.wikipedia.org",
  "requires": {
    "tools": [],
    "engines": [],
    "auth_domains": [],
    "features": [],
    "competitive": false
  },
  "eval": {
    "type": "structured_output",
    "schema": {
      "type": "object",
      "properties": {
        "engines": {
          "type": "array",
          "minItems": 3,
          "items": {
            "type": "object",
            "properties": {
              "name": {
                "type": "string"
              },
              "release_year": {
                "type": "string"
              }
            },
            "required": [
              "name",
              "release_year"
            ]
          }
        }
      },
      "required": [
        "engines"
      ]
    }
  },
  "timeout_ms": 60000,
  "max_budget_usd": 0.5,
  "tags": [
    "workflows",
    "wikipedia",
    "live",
    "multi-search",
    "browsers",
    "structured"
  ]
}