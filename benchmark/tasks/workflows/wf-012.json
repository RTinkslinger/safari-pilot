{
  "id": "wf-012",
  "category": "workflows",
  "difficulty": "hard",
  "intent": "On the fixture search form, search for a term, extract the results from page 1, click to page 2 and extract those results, then click to page 3 and extract those results. Return all results combined.",
  "start_url": "http://localhost:9876/forms/search-form.html",
  "requires": {
    "tools": [],
    "engines": [],
    "auth_domains": [],
    "features": [],
    "competitive": false
  },
  "eval": {
    "type": "structured_output",
    "schema": {
      "type": "object",
      "properties": {
        "all_results": {
          "type": "array",
          "minItems": 9,
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "all_results"
      ]
    }
  },
  "timeout_ms": 60000,
  "max_budget_usd": 0.4,
  "tags": [
    "workflows",
    "pagination",
    "fixture",
    "search",
    "multi-page",
    "aggregation"
  ]
}